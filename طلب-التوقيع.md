# طلبُ SignPath Foundation — محراب

> **ما عليك فعلُه**: افتح <https://signpath.org/apply> (أو الرابطَ في `signpath.org`)
> وانسخ الإجابات أدناه. لا أستطيع التقديمَ نيابةً عنك: الطلبُ يربط هويّتَك بالمشروع
> ويلزمه تفعيلُ مصادقةٍ ثنائيّة على حسابك — وهذان قرارُك لا قراري.

---

## الحقول

**Project name**
```
Mihrab (محراب)
```

**Project URL**
```
https://github.com/almihrab/mihrab
```

**Download page**
```
https://github.com/almihrab/mihrab/releases
```

**License**
```
MIT — https://github.com/almihrab/mihrab/blob/main/LICENSE
```

**Short description** (للنسخ كما هو)
```
Mihrab is an Arabic-first code editor built on VSCodium (patched, not forked).
It adds first-class right-to-left support to the editor and workbench — a level
of RTL support that cannot be achieved by an extension alone — and ships an
Arabic UI by default. It bundles the Sad language toolchain (editor extension,
LSP server, compiler and runner) as a reference application of the platform.

Everything shipped is open source: the editor (MIT, almihrab/mihrab) and the
bundled Sad toolchain binaries (MIT, sadlang/sadlang, a C++/LLVM compiler).
```

**Artifacts to be signed**
```
- MihrabSetup-<version>.exe   (Windows installer)
- Mihrab.exe and the accompanying binaries inside the distribution
```

**Code signing policy URL**
```
https://github.com/almihrab/mihrab/blob/main/docs/سياسة-توقيع-الشيفرة.md
```

**CI system**
```
GitHub Actions
```

---

## الشروطُ ومطابقتُنا لها — للنسخ في خانة الملاحظات إن طُلبت

| شرطُهم | حالُنا |
|---|---|
| رخصةٌ معتمَدةٌ من OSI بلا ازدواجٍ تجاريّ | ‏MIT في `almihrab/mihrab` وفي `sadlang/sadlang` معًا |
| لا برمجيّاتٍ خبيثة | محرّرُ شيفرةٍ لا غير |
| لا مكوّنَ مملوكًا غيرَ مفتوح | كلُّ ما نبنيه مفتوح — انظر «الشيءُ الوحيد» أدناه |
| مشروعٌ مُصانٌ فعلًا | دفعٌ يوميٌّ في المستودعين |
| صدر بالصيغة المراد توقيعُها | إصداراتٌ منشورةٌ على GitHub |
| وظيفتُه موصوفةٌ في صفحة التنزيل | ‏README ووصفُ الإصدار |
| فريقُ التوقيع = فريقُ التطوير | نعم — المستودعُ والإصدارُ والتوقيعُ في يدٍ واحدة |
| لا يُوقَّع إلّا ما بُني من مصدرنا | نعم — الرُقَعُ كلُّها معلَنةٌ في `patches/` ووسمُ المنبع مثبَّتٌ في `upstream.json` |
| سياسةُ توقيعٍ على صفحة المشروع | ‏`docs/سياسة-توقيع-الشيفرة.md`، وموصولةٌ من `README.md` |
| مصادقةٌ ثنائيّةٌ لكلّ عضو | **يلزمك تفعيلُها على GitHub وعلى SignPath قبل التقديم** |
| موافقةٌ يدويّةٌ على كلّ إصدار | منصوصٌ عليها في السياسة |
| بياناتُ منتَجٍ مفروضةٌ على الموقَّع | مقيسةٌ بفحص `[BR-04]` في كلّ بناء |

---

## الشيءُ الوحيدُ الذي يستحقّ الذِّكرَ ابتداءً

**اذكره في الطلب بنفسك؛ اكتشافُه لاحقًا أسوأُ من الإقرار به الآن.**

محرابٌ بناءٌ لـVS Code عبر VSCodium، فيرث إضافاتِه المدمجةَ ومعها ثنائيّان لم نبنِهما
ولا مصدرَ لهما عندنا:

```
resources/app/extensions/microsoft-authentication/dist/msalruntime.dll
resources/app/extensions/microsoft-authentication/dist/msal-node-runtime.node
resources/app/extensions/ms-vscode.js-debug/src/win32-app-container-tokens.*.node
```

نصُّ اقتراحٍ للطلب:

```
Mihrab is a VS Code / VSCodium distribution, so it inherits VS Code's built-in
extensions. Two of those ship prebuilt Microsoft binaries that we do not build
from source: the MSAL runtime used by the microsoft-authentication extension,
and a native helper in js-debug. They are present in every VSCodium build.

If this is a blocker, we can drop the microsoft-authentication extension from
our distribution — Mihrab uses the Open VSX marketplace, not the Microsoft one,
so it has no function for our users. Please tell us if you would prefer that.
```

**وهذا عرضٌ نستطيع الوفاءَ به**: لدينا أصلًا آليّةُ إسقاطِ إضافاتٍ مدمجة في سلسلة
البناء (`patch_walkthroughs_drop.py` نموذجًا)، وإسقاطُ `microsoft-authentication` لا
يخسر مستخدمَ محرابٍ شيئًا لأنّ سوقَه Open VSX.

وثانٍ أخفُّ: خطُّ **Kawkab Mono** تحت رخصة **OFL** — حرّةٌ ومعتمَدةٌ من FSF لكنّها ليست
في قائمة OSI. أصلٌ لا شيفرة، وذِكرُها سطرٌ واحد.

---

## قبل الضغط على «إرسال» — أربعةٌ

1. **فعِّل المصادقةَ الثنائيّة** على GitHub (شرطٌ صريح).
2. **ادفع وثيقةَ السياسة** — موجودةٌ محليًّا ولم تُدفَع بعد.
3. **راجع اسمَ الناشر**: كتبتُ `Mihrab` في بيانات الثنائيّات. إن أردتَ غيرَه
   فهو ثابتٌ واحدٌ في `build/patch_win_metadata.py` (`COMPANY`).
4. **أصدِر بناءَ 1.126** إن لم يكن منشورًا — يشترطون أن يكون المشروعُ قد صدر
   بالصيغة المراد توقيعُها.

---

## المدّة

من أيّامٍ إلى أسابيع. ولا شيءَ في الخطوة الثانية (بناءُ المثبِّت) ينتظر جوابَهم.
